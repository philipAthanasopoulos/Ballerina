<%= form_with(model: shootout) do |form| %>
  <% if shootout.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(shootout.errors.count, "error") %> prohibited this shootout from being saved:</h2>

      <ul>
        <% shootout.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.submit %>
  </div>
<% end %>
